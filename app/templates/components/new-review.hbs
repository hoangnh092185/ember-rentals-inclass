{{#if addNewReview}}
<h1>New Review</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="author">Author</label>
        {{input value=author id="author" placeholder="name"}}
      </div>
      <div class="form-group">
        <label for="rating">Rating</label>
        {{input value=rating id="rating" placeholder="1 to 5"}}
      </div>
      <div class="form-group">
        <label for="content">Content</label>
        {{input value=content id="content" placeholder="review"}}
      </div>
      <button {{action 'saveReview'}}>Save</button>
    </form>
  </div>
{{else}}
  <button {{action 'reviewFormShow'}}>New Review</button>
{{/if}}
